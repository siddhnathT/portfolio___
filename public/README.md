Place your resume PDF here as `resume.pdf` so the "Download CV" button works.

Example:
- `public/resume.pdf` -> accessible at `https://your-site.com/resume.pdf` or `http://localhost:5173/resume.pdf` during dev.